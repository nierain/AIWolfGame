# 项目上下文

## 项目定位

- 上游项目：`hikariming/AIWolfGame`
- 当前 Fork：`nierain/AIWolfGame`
- 最终产品：12 个座位中的 1 个由真人控制，另外 11 个由 AI 控制的本地单人狼人杀。
- 默认正式板子：预言家、女巫、骑士、守卫、4 平民、3 普通狼人、1 狼美人。
- 新增板子「镜隐迷踪」：3 小狼 + 1 觉醒隐狼 vs 魔镜少女 + 守卫 + 女巫 + 猎人 + 4 平民（详见下文）。
- 胜负采用屠边：狼人全灭则好人胜；神职全灭或平民全灭则狼人胜。

## 不可破坏的架构原则

1. 游戏规则引擎是法官，负责合法性、投票、平票、死亡、连锁死亡、警徽和胜负。
2. AI Provider 只回答“玩家想做什么”，不能直接修改游戏状态。
3. 每个玩家只能通过统一可见状态入口获取其理论上可见的信息。
4. 原有 API AI 命令行模式需要保留；后台 Codex 是新增 Provider，不是替换原模式。
5. 本地优先，继续使用 JSON/文件保存，不引入服务器数据库或微服务。

## 2026-09-15 已完成

- 保留原命令行模拟器，并为它增加 `GameController.get_visible_state(player_id)`，AI 调用不再直接接收完整身份表和私密夜间历史。
- 新增独立的真人对局状态机 `game/human_game.py`，网页层只提交行动。
- 支持随机/指定真人座位，身份默认随机，调试模式可指定全部现有身份。
- 真人可参与狼人夜聊与刀人、狼美人魅惑、预言家查验、女巫救/毒、守卫守护、骑士决斗、公开发言、警长竞选/退水/投票、放逐投票、警徽移交/撕毁。
- 完成标准 12 人板、屠边胜负、狼美人死亡殉情、警长 1.5 票、警长和放逐平票 PK/复投。
- 新增每个 AI 独立的轻量人格和持久化记忆。
- 新增可替换 Provider 接口、离线内置 AI，以及 `codex_bridge/tasks` / `responses` 文件桥接。
- 网页存档使用 schema 2，每次操作原子写入 `.panel_game_state.json`，刷新与程序重启可恢复。
- 网页改为按玩家逐步推进，不会一次刷出整轮发言。
- 修复直接双击 HTML 时只显示黑屏的问题；现在会显示正确启动提示，并提供 `启动狼人杀.bat` 一键启动入口。
- 一键启动使用系统分配的可用端口，避免 Windows 对固定 8765 端口报权限错误；手动指定端口失败时服务也会自动回退。
- 上警选择、警长投票、放逐投票和复投改为所有玩家先秘密决定，再一次性公布结果；真人不再逐个确认电脑玩家。
- 警长可选择从上家逆序或从下家顺序发言，并固定最后一个发言；警上候选人在座位区有绿色“上警”标记。
- 骑士不再有独立的白天开场询问；整段白天发言期间均可发动决斗，电脑骑士也会在每次公开发言后自行判断。
- 新增直接调用本机已登录 Codex CLI 的 AI Provider；每次只传入当前座位可见状态并要求结构化行动，主启动器默认使用此模式。
- 新增 `tests/test_human_game.py` 回归测试。
- `wolf_can_self_kill` 规则默认对真实模式和测试模式开启；开局页不再暴露单独的自刀选项，旧版底层参数仍保留以兼容回归测试。
- 新增赛后全员复盘：胜负确定后立即公开全部身份，12个座位（含已出局玩家）依次分享感想、关键判断与全局思路，全部完成后才进入最终结算页。
- 旧版已经停在 `game_over` 且从未复盘的存档，重新加载后会自动迁移到赛后全员复盘；已经完成过复盘的存档不会重复触发。
- 新增 `tools/bridge_worker.py`：文件桥接的 Worker 端命令行（`pending` / `wait` / `show` / `reply` / `schema` / `stats`），只读取任务文件里的隔离视角，并在写回前校验行动字段与目标合法性。
- 新增 `tools/launch_panel.py` 与 `启动狼人杀-桥接.bat`：以 `--provider codex` 启动桥接模式面板，端口固定 18765，便于外部 Worker 定位服务。
- 网页在 `pending.waiting_external` 为真时自动重试「继续」，真人不再需要反复点击来确认 Worker 有没有写回行动。

## 2026-09-18 已完成：新增板子「镜隐迷踪」

**板子构成**（12 人）：狼人 ×3 + 觉醒隐狼 ×1；好人 = 魔镜少女 + 守卫 + 女巫 + 猎人 + 平民 ×4。

**新增角色规则**：

- **魔镜少女（mirror_maiden）**：每晚查验一名玩家，返回其**具体身份**（女巫/守卫/狼人…，而非预言家式的"好人/狼人"）。
- **觉醒隐狼（hidden_wolf）**：属狼阵营。第 1 晚学习一名玩家（不能学自己，终身固定），获得其身份+技能：
  学女巫→一瓶"救不活的毒"；学预言家→第 2 夜起可查验；学守卫→可守护（且能挡毒）；学猎人→死后可开枪；学平民→平民；学狼人→带刀可一夜双刀。
  与三只小狼**互不知身份**；入夜时小狼全灭才获得狼刀；学狼人时双刀（两个不同目标），学别的带刀只单刀。
  魔镜查验隐狼时显示**它学到的角色**（学民显示民、学女巫显示女巫）。
- **猎人（hunter）**：死亡时可开枪带走一人；被放逐可开枪，被毒死不能开枪（与真猎人一致）。

**实现要点**：
- `game/human_game.py` 引入 `board` 参数（`classic`/`mirror`），`create_game(..., board=...)` 选择牌堆；`BOARD_DECKS`/`BOARD_NAMES`/`MIRROR_DECK`/`HIDDEN_WOLF_LEARNABLE` 常量。
- mirror 板子夜间链：隐狼学习 → 小狼聊刀 → 小狼投票 →（隐狼带刀时）隐狼刀 → 魔镜查验 → 隐狼继承技能 → 守卫 → 女巫 → 结算。双刀在 `_resolve_night` 用 `wolf_targets` 列表结算。
- 信息隔离：隐狼 `wolf_teammates` 恒为空、看不到小狼；小狼也看不到隐狼。
- 胜负屠边通用：mirror 的神职集合（魔镜/守卫/女巫/猎人）已在 `GOD_ROLES`，`_winner` 无需特判。
- 面板 setup 页新增「板子」下拉，debug 身份下拉加入 hunter/mirror_maiden/hidden_wolf；`panel_game.py` 的 `start_game` 透传 board。
- builtin provider 补齐新动作（hidden_learn/hidden_blade/hidden_skill/mirror_peek/hunter_shoot），无模型时也能跑。
- 修复镜隐迷踪隐狼视角：隐狼即使由真人控制，也看不到小狼夜聊内容、发言者或当前思考者；完成第1晚学习后，私有视角立即显示法官告知的已学习身份，页面同步显示“已学习身份”。
- `codex-cli` 默认固定使用桌面端共享的 `~/.codex` ChatGPT 登录目录；如需隔离的 CLI 账号，可用 `AIWOLF_CODEX_HOME` 显式覆盖，凭据不会写入项目。
- 修复模型模式长时间像卡住的问题：Codex/通用 CLI/API 每次请求只推进一个 AI 行动并保存；夜间与秘密决定由页面接续推进，真人回合停止，出错时停止自动重试。页面显示等待秒数，状态接口忙时快速返回 503，避免刷新请求长期排队。
- 所有板子的小狼新增白天发言阶段随时「自爆」动作和网页按钮：立即翻牌狼人、结束白天并进入黑夜；第一天警上自爆将警长竞选延迟到第二天，第二天警上再次自爆则吞掉警徽并取消后续竞选；觉醒隐狼和狼美人不可自爆。
- 本次验证：UTF-8 模式下 73 条回归通过；页面调度 4 项断言通过；实际启动服务并验证首页、健康与状态接口；真实 Codex 隔离行动成功。已结束三个重复旧服务，保留原存档备份，修复后的服务运行于 `http://127.0.0.1:51358`，对局已到真人上警选择。
- 测试新增 `MirrorBoardTests`（14 条），总数 59→81；新增自爆、警上延迟/吞警徽和禁用角色回归覆盖。
- 修复镜隐迷踪页面仍显示经典板子标题：公开状态现在明确传递 `board`，页面标题、副标题和设置页说明会随经典板/镜隐迷踪切换；当前存档接口确认 `board=mirror`，面板已重启并验证首页与健康接口返回 200。
- 明确魔镜少女的 AI 定位：作为强化版预言家主动上警和公开报验，`mirror_peek` 结果必须使用真实具体身份，禁止说成“好人/狼人”二分；内置 AI 会优先上警、避免重复查验并按具体身份发言，Codex/桥接提示同步加入同一规则。新增回归覆盖，测试总数 84。
- 修复 AI 思考期间刷新页面误回到设置页：`/api/state` 忙时返回最近一次原子保存的公开状态，前端保留公开状态并自动重试，不再把临时等待误报成未连接或新局。面板已重启验证镜隐迷踪存档仍在，健康接口返回 200。
- 修复觉醒隐狼学习猎人后的死亡触发：此前死亡结算只识别真实 `hunter` 身份，导致学习猎人的隐狼在遗言后直接进入下一阶段；现在放逐或被刀时会进入 `hunter_shoot`，被毒仍不能开枪。新增回归测试，测试总数 85；未修改当前进行中的存档。
- 增加死亡观战视角：真人觉醒隐狼出局后可以查看已记录的小狼夜聊，活着时仍看不到小狼身份、发言者和思考状态；AI 视角不开放这项死后信息。

## 2026-09-16 已完成

- 建立12名固定电脑玩家档案，每局随机抽取11名与真人组成12人局；姓名、头像、人格、说话风格和跨局记忆属于同一个玩家，座位变化时会一起移动，未参赛者不增加当局战绩。
- 12名电脑玩家依次为牢翔、牢杰、牢军、牢宝、牢恒、牢桐、牢坤、牢熙、牢灵、牢林、牢威、牢咕，集中配置在 `game/ai_profiles.py`。
- 新增本地完整对局封存 `game_archives/<game_id>.json`，每局完成赛后复盘后自动保存一次；当前已有完成存档已作为第一局封存。
- 新增 `.aiwolf_long_term_memory.json`：每名AI只保留胜负统计、最近5局摘要和自己的赛后复盘，模型请求最多携带最近3局。
- 移除每个AI记忆中重复保存的整份公共事件流水；AI请求继续从隔离视角读取当局公共历史，长期记忆不再把相同历史复制11份。
- 网页座位卡和当前行动区显示固定头像与临时名字；完整档案、头像和长期记忆均不影响真实身份的信息隔离。
- 新增真人玩家“牢雨”的固定头像；AI隔离视角只把牢雨当作普通公开玩家，不会收到真人标识或其他玩家的档案ID。
- 精简玩家座位卡：去除存活/出局文字和“本轮暂无发言”，座位号改为左上角铭片；真实身份与私人标记共用名字下方的单一身份铭牌，未知时显示“未知”，点击头像或身份铭牌均可修改私人标记；圆形发言按钮可将公共记录滚动并高亮到该玩家本轮最新发言。

## 2026-09-17 已完成

- 赛后复盘要求每位AI在核对最终身份表后，自然评价2至4位印象特别的选手；压缩评价按固定玩家档案写入最近对局记忆，座位变化后仍能对应到同一人。
- 全员复盘结束后新增12人MVP秘密票选：每人一票、允许公正自投、必须附一到两句简短理由，所有票与理由同时公开；最高票并列时共同当选。
- MVP票型、理由和结果写入完整对局封存，页面结算区显示当选者与票数汇总；尚未票选的当前或旧版完成存档会回到MVP票选，补投后原地更新封存与长期记忆且不重复累计场次。
- 真人提交MVP票后立即保存并返回页面，11名AI随后逐票自动生成且页面显示收集进度；空白理由会在输入框就地提示，避免按钮看似无响应。
- 赛后AI提示明确以公开的最终身份表为准，避免被遗言中的虚假身份信息带偏。
- 新增通用 CLI Provider（`--provider cli`）：把 `codex-cli` 里与 Codex 无关的部分抽为模块级共享资产（`action_schema` / `action_prompt` / `validate_intent` / `parse_action_payload`），`CodexCLIProvider` 改为复用它们并保留原静态方法作为兼容入口。
- `CliBackend` 以声明式描述每个后端的可执行文件名、argv 模板、提示词投递方式和模型参数位，`build_argv()` 是纯函数，不需要安装对应 CLI 即可测试。
- 首批接入三个后端：`ollama`（`run <model> --format json`，stdin 传提示词）、`claude`（`claude -p --output-format json --max-turns 1`，自动拆 `{result: ...}` 信封）、`gemini`（`-p <提示词>`）。
- `GenericCLIProvider.name = "cli"`，不是 `codex`，因此不受 `human_game.py:1081` 那个按名字跳过骑士决斗的硬编码影响。
- 不合法行动会按 `--cli-retries` 重试并附带纠正提示；传输层错误（超时、非零退出）不重试，避免把等待时间翻倍。`parse_action_payload` 能剥离 markdown 围栏与前后废话。
- 新增 `--model`（`--codex-model` 为别名）、`--cli`、`--cli-args`、`--cli-timeout`、`--cli-retries`；`create_provider` 遇到缺失的可执行文件时提示 friendly 信息并非零退出，而不是抛 traceback。
- 新增 `split_cli_args`：Windows 下不再用 POSIX `shlex.split`（会吃掉 `C:\...` 的反斜杠），只识别双引号。
- 新增 `启动狼人杀-本地模型.bat` 作为 `cli` 模式的一键入口。
- 测试从 41 条增加到 50 条：覆盖各后端 argv 展开、信封/围栏/废话的 JSON 提取、非法目标重试、名字不关骑士决斗、Windows 参数切分，以及一条**真实子进程**端到端用例（首次决策走真 CLI，其余回退内置 AI 以保持 1 秒内跑完）。
- 新增 `summarize_cli_error()`：CLI 失败时优先取 `ERROR:` 行并识别"额度用尽/凭据失效"两类，输出一行可操作的提示。此前 Codex 会把整个提示词回显在 stderr 里，`[-600:]` 截取的结果几乎全是 payload，真正原因被埋掉（2026-09-17 实际踩到：额度锁到 10-17，界面上只看到一大段发言文本）。测试增至 51 条。
- 新增 HTTP API Provider（`--provider api`，PROJECT_CONTEXT 下一步优先级 2 落地）：走标准 OpenAI 兼容 `/chat/completions`，**纯 `urllib` 实现，不引入新依赖**。
- `load_api_profile()` 复用 `config/ai_config.json` 的既有结构（`ai_players.<NAME>.{baseurl, api_key, model, timeout}`），无需重新录入；优先级为 命令行参数 > `AIWOLF_BASE_URL`/`AIWOLF_API_KEY`/`AIWOLF_MODEL` 环境变量 > 配置文件，所以**只给 key 不改文件也能跑**，也支持把 key 留在环境变量里不落盘。
- `ApiProvider` 把角色提示词与输出结构放进 system 消息、把该座位的 `visible_state` 放进 user 消息；`json_object` / `json_schema` / `none` 三种结构化输出可选，接口对 `response_format` 返回 400 时自动降级重试。
- 新增 `ProviderFailure`：区分"重试也没用"的失败（key 失效、额度用尽、连不上、超时）与"这次输出不合法"，前者立即抛出、不再空转重试。新增 `summarize_api_error()` 按 HTTP 状态给出可操作提示。
- 新增 `启动狼人杀-API.bat`。测试增至 58 条，并修正了两处测试性能问题：`serve_forever()` 默认 0.5 秒轮询导致 `shutdown()` 白等（改 `poll_interval=0.02`），以及本机 HTTP 代理会拦截 `127.0.0.1` 请求（测试内用 `no_proxy` 绕过）。
- 新增 `tools/auto_advance.py`：循环 POST `{"action":"continue"}` 代替人工点击，用于"没人盯着页面"的场景。
  它有三个关键行为：轮到自己操作时自动让开（真人回来操作完自动接手）、对局结束自动退出、**只打印天数和阶段不打印任何身份**
  （所以那个终端本身不会剧透）。实测：builtin 模式下上警阶段正确让开，真人操作后 6 秒内自动推进了 5 条记录。
  注意 `--port 0` 会让系统随机分配端口导致脚本找不到服务，用它的场景必须指定固定端口。
- 页面标题栏新增「自动推进」开关 + 间隔下拉（2/4/8/15 秒），设置存 localStorage。
  `scheduleBridgePoll()` 里新增的规则是：`can_human_act` 时**一律提前 return**（真人回合绝不自动推进），
  然后在原有 `waiting_external`/MVP 分支之后追加 `autoAdvance` 分支。
  验证方式：把页面内联脚本抽出来，在 Node 里用 DOM 桩跑真实代码，断言 7 条规则
  （开关开/关、真人回合不推进、桥接轮询与 MVP 收集未被破坏、game_over 不推进、到点真的触发 continue）全部通过。
  这个"抽脚本 + DOM 桩 + 断言调度规则"的办法，以后改页面交互逻辑可以复用。
- 修复页面 bug：**轮询撞上真人回合时页面不刷新**。实战症状是 AI 发言看起来"卡住没发出来"——
  2026-09-17 实测 9 号警上发言其实已入库（history #9），游戏也正确推进到真人，
  但面板停在旧视角并显示红色报错「当前必须等待真人操作」。
  根因：页面轮询 `继续`，当某个 AI 动作被消费后下一位恰好是真人时，下一次轮询会
  `advance_ai` → 抛「当前必须等待真人操作」→ HTTP 400，而 `run()` 的 catch 只写错误文本、不刷新 state。
  修复：新增 `refreshState()`（GET `/api/state` 并 render），catch 里识别"轮到真人"这类消息后
  清掉报错、把 actor-hint 改成「轮到你操作了。」并刷新状态。刷新后 `can_human_act` 为真，
  `scheduleBridgePoll` 自然提前 return，不再刷屏报错。同样用 DOM 桩验证（5 项断言全过）。
- **修复桥接模式下 AI 骑士永不决斗**（rain 要求在实战中改）：`human_game.py:_maybe_ai_knight_duel` 原来
  `or getattr(self.provider, "name", "") == "codex"` 直接 return False。现在去掉这个按名字的判断，
  只保留 `knight_used` 检查。
  **关键机制**：这个决斗是**同步调用**——引擎在每次公开发言之后立刻问骑士，等不了异步回写。
  所以桥接模式下要让骑士真的动手，必须在引擎发问**之前**把决定写进 `codex_bridge/responses/<task_id>.json`。
  task_id 是可预计算的：`sha256(f"{game_id}|{day}|{phase}|{seat}|knight_decide|{sequence}")[:24]`，
  其中 `sequence = state["action_seq"] + 1`（发言被应用时就地取值）。没预写时返回 None，
  自然视作"这一拍不决斗"（会留一个孤儿任务文件，无副作用）。
  改完必须**重启服务**（Python 已加载模块），重启不影响存档——`load_state()` 每次请求都从磁盘读。
  59 条回归测试全绿（`test_ai_knight_may_duel_after_any_public_speech` 用的是 builtin provider，不受影响）。

## 2026-09-19 已完成

- 修复女巫夜间用药的结算边界：统一行动窗口使用“药瓶（不用/解药/毒药）+目标”选择，解药目标可从当晚全部狼刀中选一名，并在规则引擎结算层兜底保证一晚最多一瓶；旧版双阶段存档即使同时残留 `saved` 与 `poison` 也不会双结算。镜隐迷踪的继承守卫纳入守护判定，双刀时解药只保护实际选中的狼刀目标；同守同救继续触发奶穿并出局。

- 将 `codex-cli` 与通用本地模型 `cli` 合并为网页设置中的一个 CLI 模式，可选择 Codex、Ollama、Claude 或 Gemini 后端；旧的 `BuiltinAIProvider` 仅保留给确定性回归测试，不再作为用户可选模式。
- 面板统一为单一 `启动狼人杀.bat` 入口；删除 API、本地模型、桥接三个重复批处理入口。网页设置现在选择 CLI、Codex 文件桥接或 OpenAI 兼容 API 三种模式，并把不含密钥的模式配置写入存档以支持重启续局。
- 修复 Codex ChatGPT 登录模式在设置页留空模型时传入 `None` 的问题：现在优先读取 `CODEX_HOME/config.toml` 的 `model`，缺省使用 `gpt-5.6-sol`，并始终向 `codex exec` 传递明确的 `--model`。
- Windows 下 Codex CLI 优先选择桌面端 `CODEX_CLI_PATH`/`codex.exe`，避免误选 npm `codex.cmd` 包装器导致 `os error 5`。
- 修复旧存档 `provider_config.model = null` 被归一化为字符串 `"None"` 的问题；CLI/API 模型字段现在正确保留为空值并回退到 Provider 默认模型。
- 开局页重做：右上角齿轮设置集中管理 AI 模式、CLI 后端、真实/测试模式和主题；默认 CLI + 浅色并记住选择。真实模式强制随机座位/身份，测试模式才显示可选座位/身份；开局页移除自刀选项并显示牢雨头像。
- 开局页布局微调：标题与副标题置顶，牢雨头像独立放在左侧，板子/测试模式下的座位与身份/开始游戏在右侧纵向排列；移除模式提示条和“真人玩家”副标题，字段简化为“座位”“身份”。
- 开局页对齐细节：头像在左侧区域相对右侧选项组垂直居中，测试模式的板子、座位、身份、开始游戏沿用统一的纵向间距。
- 修复镜隐迷踪 AI 套用预言家话术：镜隐迷踪规则明确写入可见状态，提示词强制说明本板没有预言家/骑士/狼美人，魔镜少女是独立的具体身份查验信息位；禁止默认使用“首验”“警徽流”等经典板模板，内置 AI 发言也改为围绕具体身份查验与票型。已将当时错误推进中的镜隐迷踪对局重置回开始页。
- 开局后交互细节：AI 思考期间不再禁用标记身份、自动推进开关和新游戏等页面控件；思考状态白天显示“X号+选手名”，夜间按狼队/守卫/女巫/查验位等行动类别显示，避免暴露身份。私人身份标记按当前板子切换角色选项，觉醒隐狼私有信息显示“已学习X号身份：具体身份”。
- 修复 AI 思考期间“重开游戏”被长时间占用的问题：`new_game` 使用独立重置锁和请求代号，立即写入新的 setup 存档；旧一轮 AI 请求完成后会丢弃结果，不会覆盖新局。
- 赛后全员复盘的发言顺序固定为真人先发言，随后再按座位顺序轮到电脑玩家；MVP 票选流程不变。
- 所有 CLI、桥接和 API AI 的统一提示词都明确要求认真阅读并回应公开历史中的真人发言；不额外暴露真人座位或隐藏视角。

## 2026-09-19 已完成（陪玩实战修复：觉醒隐狼）

真人当觉醒隐狼时暴露的三个缺口，都已修复并补上回归测试：

- **隐狼"终身固定"失效（根因）**：`_start_hidden_wolf_learn` 用整数座位号去查
  `hidden_wolf_learned`，而该字典的键是字符串座位号（`_apply` 写 `str(actor)`），
  判断永远为假 → 第 1 夜之后每个夜晚都重新排队询问学习。改为 `str(seat)`，
  并加 `state["day"] == 0` 硬门槛，把"只有第 1 夜可以学习"写进代码。
- **继承魔镜少女的查验没有实现**：`_mirror_start_hidden_skill` 原来只认 seer / guard / witch，
  学到魔镜少女等于白学。新增 `skill = "peek"` 分支，第 2 夜起在狼刀结算之后轮到隐狼查验，
  结果按 `{"day", "seat", "shown_role"}` 存进 `hidden_wolf_checks`，只进隐狼自己的 `private`。
  "隐狼按学到的身份显示"的逻辑抽成 `_peek_shown_role()`，与 `mirror_peek` 共用。
- **面板看不到查验结果**：`renderPrivate` 只渲染 `seer_checks`，于是隐狼继承的查验结果、
  以及真人当魔镜少女时自己的查验结果都无处显示。补上 `hidden_checks`（兼容预言家的
  `is_wolf` 与魔镜少女的 `shown_role`）和 `mirror_peeks`，并给 `hidden_learn` / `hidden_skill`
  补上按钮文案。

验证：`python -m unittest discover -s tests` 93 项全过；另外离线跑 20 局镜隐迷踪全部打到终局
（无死锁），其中 1 局实际触发继承查验；面板改动用 Node + DOM 桩执行真实页面脚本断言 7 项全过。

**存档修复（第 2 夜重打）**：误学把"10 号 = 魔镜少女"覆盖成"1 号 = 狼人"，且第 2 夜已结算
（11 号被刀并开枪带走真人 9 号）。按真人选择回滚到第 2 夜开始前：剪 `history` 到 #39、
`day` 退回 1、**按保留历史重算存活**（复活 9/11，保留 8 号毒杀与 10 号放逐）、
清第 2 夜的狼聊与三只狼 `notes` 里 `day == 2` 的条目、清 `hunter_shots`，
最后调引擎自己的 `_start_night` 重建夜晚链。写盘前先在副本上推演，
确认"狼刀结算后轮到 9 号使用继承的魔镜少女查验"。备份 `.panel_game_state.before_night2_redo.json`。

**CLI 超时失败导致面板整体冻结（2026-09-19 实战修复）**：重启面板后第 2 夜推进不动，
排查发现 `/api/state` 一直返回 `stale: true`（请求锁被占）、`state` 文件十几分钟不变，
但面板进程**没有任何子进程**。根因在 `CodexCLIProvider`：它用 `subprocess.run(..., timeout=...)`，
Windows 上超时后只杀直接子进程，紧接着还会再调一次 `communicate()` 去读干净管道；
npm 的 `codex.cmd` 是包装器（`cmd.exe → node.exe → codex.exe`），超时杀掉 `cmd.exe` 后，
node 与 codex.exe 变成孤儿却继续持有继承来的管道句柄，那次 drain 永久阻塞 —— 声明的超时形同虚设，
请求锁再也不释放。进程树实证：`codex.exe 246560 ← node.exe 254812 ← PID 254484（已不存在）`，
而面板进程无子进程。修法：

- 新增 `_run_cli_with_timeout()`：自己 `Popen` + `communicate(timeout)`，超时调
  `_terminate_process_tree()`（Windows `taskkill /F /T`、POSIX `os.killpg`，并关闭我方管道），
  杀完不再触碰管道；`CodexCLIProvider` 与 `GenericCLIProvider` 都不再使用 `subprocess.run`。
- 新增 `_desktop_codex_candidates()`：优先桌面端原生 `%LOCALAPPDATA%\OpenAI\Codex\bin\*\codex.exe`，
  不再回落到 npm 的 `codex.cmd` 包装器（少一层进程，也就少一层孤儿风险）。
- 回归测试 `test_cli_timeout_aborts_the_whole_process_tree`（造一个再 fork 一层的桩，
  断言超时能返回且孙子进程被一起清掉）；`test_codex_cli_provider_returns_structured_isolated_action`
  改为桩 `subprocess.Popen`。

修好后实测**Codex 后端本身很慢/不返回**（180 秒内无结果），面板会立即返回
「Codex 玩家思考超过 180 秒，请重试。」而不是冻死；这一层属于外部服务速度，不在代码可控范围。

## 当前仍缺少

- 主启动器默认由本机 Codex CLI 驱动电脑玩家，每个行动需要一次模型调用，速度和可玩局数受账号额度影响。2026-09-17 曾遇到额度错误，2026-09-18 已实测当前桌面共享认证可成功调用；额度应实时检查，不能沿用旧报错的重置日期。
- 原 OpenAI 兼容 API Agent（`game/ai_players.py`，老命令行模拟器用）仍未迁移；新的 `api` Provider 已覆盖同一协议，但老 Agent 的自由文本 + 正则解析没有被替换，两套并存。
- `config/ai_config.json` 的 8 个条目目前全是占位符，`api` 模式要真正跑起来需要填入一个可用的 key。
- `ollama` / `gemini` 两个后端的 flag 只按当时的 CLI 版本编写，没有做能力探测；本机也没装这两个 CLI，未经真机验证。
- Codex 文件桥接协议现在已配好 Worker 端工具（`tools/bridge_worker.py`），可由外部 Agent 或脚本值守，但仍没有内置的常驻 Worker 进程。
- `human_game.py:1081` 仍按 provider 的**名字**判断是否开放 AI 骑士决斗（只有 `codex` 被跳过），应改成 provider 声明能力位。
- AI 的推理、悍跳、倒钩和多人狼人协商目前是基础策略，长期局的真实感仍可继续增强。
- 内置 AI 觉醒隐狼学到查验类技能后，**发言里不会引用自己的查验结果**：`ai_providers` 的发言模板
  只认 `seer_checks` 与 `mirror_peeks`，选查验目标时也不跳过已查验过的人。引擎规则正确，缺的是话术。
- 警长票会被 `_remember(..., "vote", ...)` 记进 `beliefs.suspects`（一张"支持"票被记成"怀疑"，方向反了）。
  修法是给 `_remember` 加 `counts_as_suspicion` 之类的开关，`sheriff_vote` 只记 `votes`；
  中途改会立刻改变 AI 行为，宜等局终再动。
- 暂未加入观战回放、手动存档槽和复杂规则开关。

## 下一步优先级

1. 填写 `config/ai_config.json` 的 key，实测 `api` 模式的延迟、费用与发言质量，据此调提示词。
2. 把 `human_game.py:1081` 的名字判断换成 provider 能力位，让骑士决斗对包括文件桥接模式在内的所有后端都可用。
3. 在严格信息隔离不变的前提下继续增强 AI 记忆、站边变化和狼人协商。
4. 根据实际试玩反馈微调警长规则、夜间顺序与页面交互。
